---
interface Props {
  children: any;
}
---

<ul>
  <slot />
</ul>
<style>
  @import "open-props/media";
  ul {
    --width: 250px;
    width: var(--width);
    height: var(--width);
    border-radius: 50%;
    background-image: url(/images/frequency.svg);
    position: relative;
    padding-inline-start: 0;
    transform: scale(0.65);
    margin-block-end: var(--size-9);
  }

  @media (--md-n-above) {
    ul {
      transform: scale(1);
      margin-block-start: var(--size-12);
      margin-block-end: var(--size-13);
    }
  }

  @media (--lg-n-above) {
    ul {
      transform: scale(1.3);
      margin-block-start: var(--size-13);
    }
  }
</style>
