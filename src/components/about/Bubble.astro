---
interface Props {
  Content: any;
  id: string;
  index: number;
}
import Dots from "../dots.astro";
const { id, Content, index } = Astro.props;
---

<section class="bubble" {id}>
  {id !== "call-to-action" && <Dots top="50px" amount={index} />}
  <Content />
</section>

<style>
  section {
    background-color: var(--purp);
    position: relative;
    container-type: size;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    border-radius: 50%;
    overflow: hidden;
    margin: var(--size-2);
    aspect-ratio: 1/1;
    color: var(--white);
    padding: var(--size-9);

    :global(strong) {
      display: block;
      font-size: var(--font-size-3);
      font-weight: var(--font-weight-7);
    }
    :global(ul),
    :globa(li) {
      margin: 0;
      padding: 0;
      text-indent: 0;
      list-style-type: 0;
    }

    :global(ul) {
      padding-block-start: var(--size-5);
    }

    @container (width > 300px) {
      padding-inline: var(--size-10);
    }
  }
</style>
